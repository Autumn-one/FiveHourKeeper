<UserControl x:Class="FiveHourKeeper.Views.ModelListPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:m="clr-namespace:FiveHourKeeper.Models">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="模型管理" Style="{StaticResource TitleStyle}" />
                <TextBlock Style="{StaticResource HintStyle}" Text="每个模型独立保存协议、密钥与重置时间；操作按钮在每行末尾。" Margin="0,4,0,0" />
            </StackPanel>
            <Button Grid.Column="1" Style="{StaticResource PrimaryButton}" Content="＋ 新增模型"
                    Command="{Binding AddCommand}" VerticalAlignment="Center" />
        </Grid>

        <Border Grid.Row="1" Margin="0,16,0,0" Style="{StaticResource CardStyle}" Padding="0">
            <DataGrid ItemsSource="{Binding Profiles}" SelectedItem="{Binding Selected}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="别名" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="协议" Binding="{Binding Protocol, Converter={StaticResource EnumDisplay}}" Width="110" />
                    <DataGridTextColumn Header="模型" Binding="{Binding ModelName}" Width="*" />
                    <DataGridTextColumn Header="重置时间" Binding="{Binding ScheduleSummary}" Width="110" />
                    <DataGridTemplateColumn Header="状态" Width="64">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource BadgeStyle}" Width="48" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Enabled, Converter={StaticResource EnabledText}}"
                                               FontSize="12" HorizontalAlignment="Center" Foreground="{Binding Enabled, Converter={StaticResource EnabledTone}}" />
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="操作" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Style="{StaticResource RowButton}" Content="编辑"
                                            Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" />
                                    <Button Style="{StaticResource RowDangerButton}" Content="删除"
                                            Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>